export interface IPageSchema {
  headingText: string;
  pageText: string;
}

const pageSchema: IPageSchema = {
  headingText: "This is a heading",
  pageText: "This is some sample text",
};

export default pageSchema;
