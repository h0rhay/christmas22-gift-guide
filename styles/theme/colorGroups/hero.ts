import colors from "../colors";
import { ColorGroupType } from "types/colorsTypes";

const heroColorGroup: ColorGroupType = {
  surface: colors.red,
  interface: colors.milk,
};

export default heroColorGroup;
