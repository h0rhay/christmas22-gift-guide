import colors from "../colors";
import { ColorGroupType } from "types/colorsTypes";

const oliveColorGroup: ColorGroupType = {
  surface: colors.olive,
  interface: colors.milk,
};

export default oliveColorGroup;
