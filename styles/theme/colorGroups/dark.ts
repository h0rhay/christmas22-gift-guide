import colors from "../colors";
import { ColorGroupType } from "types/colorsTypes";

const darkColorGroup: ColorGroupType = {
  surface: colors.black,
  interface: colors.white,
};

export default darkColorGroup;
