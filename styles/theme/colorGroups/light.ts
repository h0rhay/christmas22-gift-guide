import colors from "../colors";
import { ColorGroupType } from "types/colorsTypes";

const lightColorGroup: ColorGroupType = {
  surface: colors.white,
  interface: colors.black,
};

export default lightColorGroup;
