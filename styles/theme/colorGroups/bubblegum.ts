import colors from "../colors";
import { ColorGroupType } from "types/colorsTypes";

const bubblegumColorGroup: ColorGroupType = {
  surface: colors.bubblegum,
  interface: colors.red,
};

export default bubblegumColorGroup;
