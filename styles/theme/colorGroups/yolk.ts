import colors from "../colors";
import { ColorGroupType } from "../../../types/colorsTypes";

const yolkColorGroup: ColorGroupType = {
  surface: colors.yolk,
  interface: colors.olive,
};

export default yolkColorGroup;
