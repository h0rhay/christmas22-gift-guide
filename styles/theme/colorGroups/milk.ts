import colors from "../colors";
import { ColorGroupType } from "types/colorsTypes";

const milkColorGroup: ColorGroupType = {
  surface: colors.milk,
  interface: colors.olive,
};

export default milkColorGroup;
